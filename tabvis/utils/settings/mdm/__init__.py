"""MDM (Mobile Device Management) settings package.

Provides shared MDM constants, plist path builders,
(:mod:`tabvis.utils.settings.mdm.constants`) and the non-blocking subprocess raw-read layer
(:mod:`tabvis.utils.settings.mdm.raw_read`). The parsing / first-source-wins ``settings`` consumer
(``mdm/settings.ts``) is not yet implemented.
"""
